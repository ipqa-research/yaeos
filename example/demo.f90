program examples
    use bench, only: benchmarks
    use hyperdual_pr76, only: adiff_pr76
    use flashing, only: run_flashes

    ! call benchmarks
    ! call adiff_pr76
    call run_flashes
end program